@using Blazor.Diagrams.Components.Renderers;

<div>
    <h5 class="card-title">@Node.Title</h5>
    <input type="number" class="form-control" @bind-value="Node.ProcessCouuntPerSec" placeholder="Count" />

    @foreach (var port in Node.Ports)
    {
        // In case you have any ports to show
        // IMPORTANT: You are always in charge of rendering ports
        <PortRenderer @key="port" Port="port" />
    }

</div>

@code {
    // This gets filled by the library
    [Parameter] public ThrottleNode Node { get; set; } = null!;

}
